import { Collapse } from "antd";
import { CollapseProps } from "antd";
import React from "react";

const SharpCollapse = (props: CollapseProps) => {
  return <Collapse {...props} />;
};

export default SharpCollapse;
